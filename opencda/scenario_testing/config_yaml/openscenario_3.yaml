description: |-
  Author: Wei Shao <phdweishao@gmail.com>
  Content: Test configurations for `openscenario_carla` that incorporates ScenarioRunner

# Parameters needed for ScenarioRunner
scenario_runner:
  town: town03
  scenario: Scenario_3
  num_actors: 2
  configFile: './opencda/scenario_testing/scenarios/scenario_3.xml'
  additionalScenario: '/home/ztu/OpenCDA/opencda/scenario_testing/scenarios/scenario_3.py'

# Define OpenCDA scenario
scenario:
  single_cav_list:
    - name: ego
      destination: [-38.8, 134.7, 0.5]
      v2x:
        enabled: true
      behavior:
        local_planner:
          debug_trajectory: false
          debug: false
    - name: cav_1
      destination: [-28.8, 137.7, 0.5] # TODO: needs change
      v2x:
        enabled: true
      behavior:
        local_planner:
          debug_trajectory: false
          debug: false

vehicle_base:
  sensing:
    perception:
      lidar:
        visualize: true
  behavior:
    max_speed: 20
    local_planner:
      debug_trajectory: false
      debug: false